import React from 'react';
import './global.css';
import { Layout } from './layout';

const Root = () => {
    return (
        <Layout>
            {/* Main application content goes here */}
        </Layout>
    );
};

export default Root;